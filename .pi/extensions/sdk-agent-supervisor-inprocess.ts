/**
 * PROTOTYPE: auto-load the in-process supervisor in this trusted worktree so the
 * experiment starts with the normal `pi` command and no wrapper executable.
 */
export { default } from "../../prototypes/sdk-agent-supervisor-inprocess/owner-extension.ts";
